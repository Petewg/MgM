
#include 'fileio.ch'

#define CRLF CHR(13)+CHR(10)

PROCEDURE CreateINI()
  LOCAL hand

  IF (hand := FCREATE('biorhythm.ini', FC_NORMAL)) != -1

    FWRITE(hand,'English,Polski,Russian,Italiano,Deutsch,Espanol,Èesky,Slovenèina,Bulgarian,Português,xTrans'+CRLF)
    FWRITE(hand,'Latina,Esperanto - not finished yet'+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[Date of birth]'+CRLF)
    FWRITE(hand,'cDOB_day=1'+CRLF)
    FWRITE(hand,'cDOB_month=1'+CRLF)
    FWRITE(hand,'cDOB_year=1970'+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[Biorhythm]'+CRLF)
    FWRITE(hand,'language=English'+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cTranslator]'+CRLF)
    FWRITE(hand,'xTrans=<Translator>'+CRLF)
    FWRITE(hand,'English=Adam Trub³ajewicz'+CRLF)
    FWRITE(hand,'Polski=Christian T. Kurowski'+CRLF)
    FWRITE(hand,'Russian=Alexey L. Gustow'+CRLF)
    FWRITE(hand,'Italiano=Arcangelo Molinaro'+CRLF)
    FWRITE(hand,'Deutsch=Aleksandra Brzozowska, David Chmara'+CRLF)
    FWRITE(hand,'Espanol=Lucyna Iwaniec, Lourdes Guijarro'+CRLF)
    FWRITE(hand,'Èesky=Lucyna Iwaniec, Ing. Vladimir Precak'+CRLF)
    FWRITE(hand,'Slovenèina=Lucyna Iwaniec, Ing. Vladimir Precak'+CRLF)
	FWRITE(hand,'Bulgarian=Danail Dikov'+CRLF)
    FWRITE(hand,'Português=Fernando C.Salvador'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cTranslators]'+CRLF)
    FWRITE(hand,'xTrans=cTranslators'+CRLF)
    FWRITE(hand,'English=Translators'+CRLF)
    FWRITE(hand,'Polski=T³umacze'+CRLF)
    FWRITE(hand,'Russian=Ïåðåâîä÷èêè'+CRLF)
    FWRITE(hand,'Italiano=Traduttori'+CRLF)
    FWRITE(hand,'Deutsch=Übersetzers'+CRLF)
    FWRITE(hand,'Espanol=Traductores'+CRLF)
    FWRITE(hand,'Èesky=pøekladatelé'+CRLF)
    FWRITE(hand,'Slovenèina=tlmoènici'+CRLF)
	FWRITE(hand,'Bulgarian=Ïðåâîäà÷è'+CRLF)
    FWRITE(hand,'Português=Tradutores'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cAbout]'+CRLF)
    FWRITE(hand,'xTrans=cAbout...'+CRLF)
    FWRITE(hand,'English=About...'+CRLF)
    FWRITE(hand,'Polski=O programie...'+CRLF)
    FWRITE(hand,'Russian=Î ïðîãðàììå...'+CRLF)
    FWRITE(hand,'Italiano=Notizie...'+CRLF)
    FWRITE(hand,'Deutsch=über das Programm'+CRLF)
    FWRITE(hand,'Espanol=Sobre algo...'+CRLF)
    FWRITE(hand,'Èesky=o programì...'+CRLF)
    FWRITE(hand,'Slovenèina=o programe...'+CRLF)
	FWRITE(hand,'Bulgarian=Çà ïðîãðàìàòà...'+CRLF)
    FWRITE(hand,'Português=Sobre...'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cVersion]'+CRLF)
    FWRITE(hand,'xTrans=cVersion'+CRLF)
    FWRITE(hand,'English=version'+CRLF)
    FWRITE(hand,'Polski=wersja'+CRLF)
    FWRITE(hand,'Russian=âåðñèÿ'+CRLF)
    FWRITE(hand,'Italiano=versione'+CRLF)
    FWRITE(hand,'Deutsch=die Version'+CRLF)
    FWRITE(hand,'Espanol=versión'+CRLF)
    FWRITE(hand,'Èesky=verze'+CRLF)
    FWRITE(hand,'Slovenèina=verzia'+CRLF)
	FWRITE(hand,'Bulgarian=âåðñèÿ'+CRLF)
    FWRITE(hand,'Português=Versão'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cBiorhythms]'+CRLF)
    FWRITE(hand,'xTrans=cBiorhythms'+CRLF)
    FWRITE(hand,'English=Biorhythms'+CRLF)
    FWRITE(hand,'Polski=Biorytmy'+CRLF)
    FWRITE(hand,'Russian=Áèîðèòìû'+CRLF)
    FWRITE(hand,'Italiano=Bioritmi'+CRLF)
    FWRITE(hand,'Deutsch=der Biorhythmus'+CRLF)
    FWRITE(hand,'Espanol=Biorritmos'+CRLF)
    FWRITE(hand,'Èesky=Biorytmy'+CRLF)
    FWRITE(hand,'Slovenèina=Biorytmy'+CRLF)
    FWRITE(hand,'Bulgarian=Áèîðèòìè'+CRLF)
    FWRITE(hand,'Português=Bioritmos'+CRLF)
	FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cDateOfBirth]'+CRLF)
    FWRITE(hand,'xTrans=cDateOfBirth'+CRLF)
    FWRITE(hand,'English=Date of birth'+CRLF)
    FWRITE(hand,'Polski=Data urodzenia'+CRLF)
    FWRITE(hand,'Russian=Äàòà ðîæäåíèÿ'+CRLF)
    FWRITE(hand,'Italiano=Data di nascita'+CRLF)
    FWRITE(hand,'Deutsch=das Geburtsdatum'+CRLF)
    FWRITE(hand,'Espanol=Fecha de nacimiento'+CRLF)
    FWRITE(hand,'Èesky=datum narození'+CRLF)
    FWRITE(hand,'Slovenèina=datum narodenia'+CRLF)
	FWRITE(hand,'Bulgarian=Äàòà íà ðàæäàíå'+CRLF)
    FWRITE(hand,'Português=Data de Nascimento'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cDateOfCalc]'+CRLF)
    FWRITE(hand,'xTrans=cDateOfCalc'+CRLF)
    FWRITE(hand,'English=Date of calculations'+CRLF)
    FWRITE(hand,'Polski=Data obliczeñ'+CRLF)
    FWRITE(hand,'Russian=Äàòà, íà êîòîðóþ âû÷èñëèòü'+CRLF)
    FWRITE(hand,'Italiano=Data di calcolo'+CRLF)
    FWRITE(hand,'Deutsch=Datum der Berechnungen'+CRLF)
    FWRITE(hand,'Espanol=Fecha de cálculo'+CRLF)
    FWRITE(hand,'Èesky=datum vypoètu'+CRLF)
    FWRITE(hand,'Slovenèina=datum vypoètu'+CRLF)
	FWRITE(hand,'Bulgarian=Äàòà çà èç÷èñëåíèÿ'+CRLF)
    FWRITE(hand,'Português=Data de Cálculo'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cCycles]'+CRLF)
    FWRITE(hand,'xTrans=cCycles'+CRLF)
    FWRITE(hand,'English=Cycles'+CRLF)
    FWRITE(hand,'Polski=Cykle'+CRLF)
    FWRITE(hand,'Russian=Öèêëû'+CRLF)
    FWRITE(hand,'Italiano=Cicli'+CRLF)
    FWRITE(hand,'Deutsch=die Perioden'+CRLF)
    FWRITE(hand,'Espanol=Ciclos'+CRLF)
    FWRITE(hand,'Èesky=cykly'+CRLF)
    FWRITE(hand,'Slovenèina=cykly'+CRLF)
	FWRITE(hand,'Bulgarian=Öèêëè'+CRLF)
    FWRITE(hand,'Português=Ciclos'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cShowAll]'+CRLF)
    FWRITE(hand,'xTrans=cShowAll'+CRLF)
    FWRITE(hand,'English=show another cycles even if the summary cycle is showed'+CRLF)
    FWRITE(hand,'Polski=poka¿ inne cykle nawet, gdy jest wyœwietlony cykl sumaryczny'+CRLF)
    FWRITE(hand,'Russian=ïîêàçàòü ÂÑÅ öèêëû (äàæå ïðè ïîêàçå òîëüêî ñóììû)'+CRLF)
    FWRITE(hand,'Italiano=Mostra altri cicli anche se è visualizzato il ciclo globale'+CRLF)
    FWRITE(hand,'Deutsch=Zeige andere Perioden, selbst wenn die summarische Periode gezeigt wird'+CRLF)
    FWRITE(hand,'Espanol=Muestra otros ciclos aunque el ciclo resumen haya sido mostrado'+CRLF)
    FWRITE(hand,'Èesky=ukaž jiné cykly, dokonce  když je zvyraznen vyslední cyklus'+CRLF)
    FWRITE(hand,'Slovenèina=ukáž iné cykly, dokonca keï je zvýraznený výsledný cyklus'+CRLF)
	FWRITE(hand,'Bulgarian=Ïîêàæè âñè÷êè öèêëè (äîðè êîãàòî ïîêàçâà îáîáùåí öèêúë)'+CRLF)
    FWRITE(hand,'Português=Mostra outros ciclos ainda que o ciclo resumido seja mostrado'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cClassicalCycles]'+CRLF)
    FWRITE(hand,'xTrans=cClassicalCycles'+CRLF)
    FWRITE(hand,'English=Classical cycles'+CRLF)
    FWRITE(hand,'Polski=Cykle tradycyjne'+CRLF)
    FWRITE(hand,'Russian=Òðàäèöèîííûå'+CRLF)
    FWRITE(hand,'Italiano=Cicli Classici'+CRLF)
    FWRITE(hand,'Deutsch=die traditionellen Perioden'+CRLF)
    FWRITE(hand,'Espanol=Ciclos clásicos'+CRLF)
    FWRITE(hand,'Èesky=klasicky cyklus'+CRLF)
    FWRITE(hand,'Slovenèina=klasický cyklus'+CRLF)
	FWRITE(hand,'Bulgarian=Òðàäèöèîííè'+CRLF)
    FWRITE(hand,'Português=Ciclos Clássicos'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cAdditionalCycle]'+CRLF)
    FWRITE(hand,'xTrans=cAdditionalCycle'+CRLF)
    FWRITE(hand,'English=Additional cycle'+CRLF)
    FWRITE(hand,'Polski=Cykl dodatkowy'+CRLF)
    FWRITE(hand,'Russian=Äîáàâî÷íûé'+CRLF)
    FWRITE(hand,'Italiano=Ciclo Addizionale'+CRLF)
    FWRITE(hand,'Deutsch=die zusätzliche Periode'+CRLF)
    FWRITE(hand,'Espanol=Ciclo adicional'+CRLF)
    FWRITE(hand,'Èesky=dodateèný cyklus'+CRLF)
    FWRITE(hand,'Slovenèina=dodatoèný cyklus'+CRLF)
	FWRITE(hand,'Bulgarian=Äîïúëíèòåëíè'+CRLF)
    FWRITE(hand,'Português=Ciclo Adicional'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cPhysicalCycle]'+CRLF)
    FWRITE(hand,'xTrans=cPhysicalCycle'+CRLF)
    FWRITE(hand,'English=Physical cycle'+CRLF)
    FWRITE(hand,'Polski=Cykl fizyczny'+CRLF)
    FWRITE(hand,'Russian=Ôèçè÷åñêèé öèêë'+CRLF)
    FWRITE(hand,'Italiano=Ciclo Fisico'+CRLF)
    FWRITE(hand,'Deutsch=die physikalische Periode'+CRLF)
    FWRITE(hand,'Espanol=Ciclo físico'+CRLF)
    FWRITE(hand,'Èesky=fyzicky cyklus'+CRLF)
    FWRITE(hand,'Slovenèina=fizycký cyklus'+CRLF)
	FWRITE(hand,'Bulgarian=Ôèçè÷åñêè öèêúë'+CRLF)
    FWRITE(hand,'Português=Ciclo Físico'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cEmotionalCycle]'+CRLF)
    FWRITE(hand,'xTrans=cEmotionalCycle'+CRLF)
    FWRITE(hand,'English=Emotional Cycle'+CRLF)
    FWRITE(hand,'Polski=Cykl emocjonalny'+CRLF)
    FWRITE(hand,'Russian=Ýìîöèîíàëüíûé öèêë'+CRLF)
    FWRITE(hand,'Italiano=Ciclo Emotivo'+CRLF)
    FWRITE(hand,'Deutsch=die emotionale Periode'+CRLF)
    FWRITE(hand,'Espanol=Ciclo emocional'+CRLF)
    FWRITE(hand,'Èesky=emoèní cyklus'+CRLF)
    FWRITE(hand,'Slovenèina=emotívny cyklus'+CRLF)
	FWRITE(hand,'Bulgarian=Åìîöèîíàëåí öèêúë'+CRLF)
    FWRITE(hand,'Português=Ciclo Emocional'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cIntellectualCycle]'+CRLF)
    FWRITE(hand,'xTrans=cIntellectualCycle'+CRLF)
    FWRITE(hand,'English=Intellectual cycle'+CRLF)
    FWRITE(hand,'Polski=Cykl intelektualny'+CRLF)
    FWRITE(hand,'Russian=Èíòåëëåêòóàëüíûé öèêë'+CRLF)
    FWRITE(hand,'Italiano=Ciclo Intellettivo'+CRLF)
    FWRITE(hand,'Deutsch=die intellektuelle Periode'+CRLF)
    FWRITE(hand,'Espanol=Ciclo intelectual'+CRLF)
    FWRITE(hand,'Èesky=intelektualni cyklus'+CRLF)
    FWRITE(hand,'Slovenèina=intelektuálny cyklus'+CRLF)
	FWRITE(hand,'Bulgarian=Èíòåëåêòóàëåí öèêúë'+CRLF)
    FWRITE(hand,'Português=Ciclo Intelectual'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cLanguage]'+CRLF)
    FWRITE(hand,'xTrans=cLanguage'+CRLF)
    FWRITE(hand,'English=Language'+CRLF)
    FWRITE(hand,'Polski=Jêzyk'+CRLF)
    FWRITE(hand,'Russian=ßçûê'+CRLF)
    FWRITE(hand,'Italiano=Lingua'+CRLF)
    FWRITE(hand,'Deutsch=die Sprache'+CRLF)
    FWRITE(hand,'Espanol=Idioma'+CRLF)
    FWRITE(hand,'Èesky=jazyk'+CRLF)
    FWRITE(hand,'Slovenèina=jazyk'+CRLF)
	FWRITE(hand,'Bulgarian=Åçèê'+CRLF)
    FWRITE(hand,'Português=Linguagem'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cButton_Phys]'+CRLF)
    FWRITE(hand,'xTrans=cButton_Phys'+CRLF)
    FWRITE(hand,'English=Physical'+CRLF)
    FWRITE(hand,'Polski=Fizyczny'+CRLF)
    FWRITE(hand,'Russian=Ôèçè÷åñêèé'+CRLF)
    FWRITE(hand,'Italiano=Fisico'+CRLF)
    FWRITE(hand,'Deutsch=Physikalisch'+CRLF)
    FWRITE(hand,'Espanol=Físico'+CRLF)
    FWRITE(hand,'Èesky=Fyzicky'+CRLF)
    FWRITE(hand,'Slovenèina=Fizycký'+CRLF)
	FWRITE(hand,'Bulgarian=Ôèçè÷åñêè'+CRLF)
    FWRITE(hand,'Português=Físico'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cButton_Emot]'+CRLF)
    FWRITE(hand,'xTrans=cButton_Emot'+CRLF)
    FWRITE(hand,'English=Emotional'+CRLF)
    FWRITE(hand,'Polski=Emocjonalny'+CRLF)
    FWRITE(hand,'Russian=Ýìîöèîíàëüíûé'+CRLF)
    FWRITE(hand,'Italiano=Emotivo'+CRLF)
    FWRITE(hand,'Deutsch=Emotional'+CRLF)
    FWRITE(hand,'Espanol=Emocional'+CRLF)
    FWRITE(hand,'Èesky=Emoèní'+CRLF)
    FWRITE(hand,'Slovenèina=Emotívny'+CRLF)
	FWRITE(hand,'Bulgarian=Åìîöèîíàëåí'+CRLF)
    FWRITE(hand,'Português=Emocional'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cButton_Inte]'+CRLF)
    FWRITE(hand,'xTrans=cButton_Inte'+CRLF)
    FWRITE(hand,'English=Intellectual'+CRLF)
    FWRITE(hand,'Polski=Intelektualny'+CRLF)
    FWRITE(hand,'Russian=Èíòåëëåêòóàëüíûé'+CRLF)
    FWRITE(hand,'Italiano=Intellettivo'+CRLF)
    FWRITE(hand,'Deutsch=Intellektuell'+CRLF)
    FWRITE(hand,'Espanol=Intelectual'+CRLF)
    FWRITE(hand,'Èesky=Intelektualni'+CRLF)
    FWRITE(hand,'Slovenèina=Intelektuálny'+CRLF)
	FWRITE(hand,'Bulgarian=Èíòåëåêòóàëåí'+CRLF)
    FWRITE(hand,'Português=Intelectual'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cButton_Intu]'+CRLF)
    FWRITE(hand,'xTrans=cButton_Intu'+CRLF)
    FWRITE(hand,'English=Intuitive'+CRLF)
    FWRITE(hand,'Polski=Intuicyjny'+CRLF)
    FWRITE(hand,'Russian=Èíòóèòèâíûé'+CRLF)
    FWRITE(hand,'Italiano=Intuitivo'+CRLF)
    FWRITE(hand,'Deutsch=Intuitiv'+CRLF)
    FWRITE(hand,'Espanol=Intuitivo'+CRLF)
    FWRITE(hand,'Èesky=intuitívny'+CRLF)
    FWRITE(hand,'Slovenèina=intuitívny'+CRLF)
	FWRITE(hand,'Bulgarian=Èíòóèòèâåí'+CRLF)
    FWRITE(hand,'Português=Intuitivo'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cButton_Summary]'+CRLF)
    FWRITE(hand,'xTrans=cButton_Summary'+CRLF)
    FWRITE(hand,'English=Summary (Physical + Emotional + Intellectual)'+CRLF)
    FWRITE(hand,'Polski=Sumaryczny (Fizyczny + Emocjonalny + Intelektualny)'+CRLF)
    FWRITE(hand,'Russian=Ñóììà (ôèçè÷åñêèé + ýìîöèîíàëüíûé + èíòåëëåêòóàëüíûé)'+CRLF)
    FWRITE(hand,'Italiano=Totale (Fisico + Emotivo + Intellettivo)'+CRLF)
    FWRITE(hand,'Deutsch=Summarisch (physikalisch + emotional + intellektuell)'+CRLF)
    FWRITE(hand,'Espanol=Resumen (Físico + Emocional + Intelectual)'+CRLF)
    FWRITE(hand,'Èesky=souhrnny, celkový'+CRLF)
    FWRITE(hand,'Slovenèina=celkový'+CRLF)
	FWRITE(hand,'Bulgarian=Îáîáùåí  (Ôèçè÷åñêè + Åìîöèîíàëåí + Èíòåëåêòóàëåí) '+CRLF)
    FWRITE(hand,'Português=Resumo (Físico + Emocional + Intelectual)'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cButton_Today]'+CRLF)
    FWRITE(hand,'xTrans=cToday'+CRLF)
    FWRITE(hand,'English=Today'+CRLF)
    FWRITE(hand,'Polski=Dzisiaj'+CRLF)
    FWRITE(hand,'Russian=Íà ñåãîäíÿ'+CRLF)
    FWRITE(hand,'Italiano=Oggi'+CRLF)
    FWRITE(hand,'Deutsch=Heute'+CRLF)
    FWRITE(hand,'Espanol=Hoy'+CRLF)
    FWRITE(hand,'Èesky=dnes'+CRLF)
    FWRITE(hand,'Slovenèina=dnes'+CRLF)
	FWRITE(hand,'Bulgarian=Äíåñ'+CRLF)
    FWRITE(hand,'Português=Hoje'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cButton_Calculate]'+CRLF)
    FWRITE(hand,'xTrans=cButton_Calculate'+CRLF)
    FWRITE(hand,'English=Calculate'+CRLF)
    FWRITE(hand,'Polski=Oblicz'+CRLF)
    FWRITE(hand,'Russian=Âû÷èñëèòü'+CRLF)
    FWRITE(hand,'Italiano=Calcola'+CRLF)
    FWRITE(hand,'Deutsch=Berechnen Sie!'+CRLF)
    FWRITE(hand,'Espanol=Calcula'+CRLF)
    FWRITE(hand,'Èesky=seèti'+CRLF)
    FWRITE(hand,'Slovenèina=zpoèítaj'+CRLF)
	FWRITE(hand,'Bulgarian=Èç÷èñëè'+CRLF)
    FWRITE(hand,'Português=Calcular'+CRLF)
    FWRITE(hand,'Latina='+CRLF)
    FWRITE(hand,'Esperanto='+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cJanuary]'+CRLF)
    FWRITE(hand,'xTrans=cJanuary'+CRLF)
    FWRITE(hand,'English=January'+CRLF)
    FWRITE(hand,'Polski=stycznia'+CRLF)
    FWRITE(hand,'Russian=ÿíâàðÿ'+CRLF)
    FWRITE(hand,'Italiano=Gennaio'+CRLF)
    FWRITE(hand,'Deutsch=Januar'+CRLF)
    FWRITE(hand,'Espanol=Enero'+CRLF)
    FWRITE(hand,'Èesky=ledna'+CRLF)
    FWRITE(hand,'Slovenèina=januara'+CRLF)
	FWRITE(hand,'Bulgarian=ÿíóàðè'+CRLF)
    FWRITE(hand,'Português=Janeiro'+CRLF)
    FWRITE(hand,'Latina=Ianuarius'+CRLF)
    FWRITE(hand,'Esperanto=Januaro'+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cFebruary]'+CRLF)
    FWRITE(hand,'xTrans=cFebruary'+CRLF)
    FWRITE(hand,'English=February'+CRLF)
    FWRITE(hand,'Polski=lutego'+CRLF)
    FWRITE(hand,'Russian=ôåâðàëÿ'+CRLF)
    FWRITE(hand,'Italiano=Febbraio'+CRLF)
    FWRITE(hand,'Deutsch=Februar'+CRLF)
    FWRITE(hand,'Espanol=Febrero'+CRLF)
    FWRITE(hand,'Èesky=unora'+CRLF)
    FWRITE(hand,'Slovenèina=februára'+CRLF)
	FWRITE(hand,'Bulgarian=ôåâðóàðè'+CRLF)
    FWRITE(hand,'Português=Fevereiro'+CRLF)
    FWRITE(hand,'Latina=Februarius'+CRLF)
    FWRITE(hand,'Esperanto=Februaro'+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cMarch]'+CRLF)
    FWRITE(hand,'xTrans=cMarch'+CRLF)
    FWRITE(hand,'English=March'+CRLF)
    FWRITE(hand,'Polski=marca'+CRLF)
    FWRITE(hand,'Russian=ìàðòà'+CRLF)
    FWRITE(hand,'Italiano=Marzo'+CRLF)
    FWRITE(hand,'Deutsch=März'+CRLF)
    FWRITE(hand,'Espanol=Marzo'+CRLF)
    FWRITE(hand,'Èesky=brezna'+CRLF)
    FWRITE(hand,'Slovenèina=marca'+CRLF)
	FWRITE(hand,'Bulgarian=ìàðò'+CRLF)
    FWRITE(hand,'Português=Março'+CRLF)
    FWRITE(hand,'Latina=Martius'+CRLF)
    FWRITE(hand,'Esperanto=Marto'+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cApril]'+CRLF)
    FWRITE(hand,'xTrans=cApril'+CRLF)
    FWRITE(hand,'English=April'+CRLF)
    FWRITE(hand,'Polski=kwietnia'+CRLF)
    FWRITE(hand,'Russian=àïðåëÿ'+CRLF)
    FWRITE(hand,'Italiano=Aprile'+CRLF)
    FWRITE(hand,'Deutsch=April'+CRLF)
    FWRITE(hand,'Espanol=Abril'+CRLF)
    FWRITE(hand,'Èesky=dubna'+CRLF)
    FWRITE(hand,'Slovenèina=aprila'+CRLF)
	FWRITE(hand,'Bulgarian=àïðèë'+CRLF)
    FWRITE(hand,'Português=Abril'+CRLF)
    FWRITE(hand,'Latina=Aprilis'+CRLF)
    FWRITE(hand,'Esperanto=Aprilo'+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cMay]'+CRLF)
    FWRITE(hand,'xTrans=cMay'+CRLF)
    FWRITE(hand,'English=May'+CRLF)
    FWRITE(hand,'Polski=maja'+CRLF)
    FWRITE(hand,'Russian=ìàÿ'+CRLF)
    FWRITE(hand,'Italiano=Maggio'+CRLF)
    FWRITE(hand,'Deutsch=Mai'+CRLF)
    FWRITE(hand,'Espanol=Mayo'+CRLF)
    FWRITE(hand,'Èesky=kvetna'+CRLF)
    FWRITE(hand,'Slovenèina=mája'+CRLF)
	FWRITE(hand,'Bulgarian=ìàé'+CRLF)
    FWRITE(hand,'Português=Maio'+CRLF)
    FWRITE(hand,'Latina=Maius'+CRLF)
    FWRITE(hand,'Esperanto=Majo'+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cJune]'+CRLF)
    FWRITE(hand,'xTrans=cJune'+CRLF)
    FWRITE(hand,'English=June'+CRLF)
    FWRITE(hand,'Polski=czerwca'+CRLF)
    FWRITE(hand,'Russian=èþíÿ'+CRLF)
    FWRITE(hand,'Italiano=Giugno'+CRLF)
    FWRITE(hand,'Deutsch=Juni'+CRLF)
    FWRITE(hand,'Espanol=Junio'+CRLF)
    FWRITE(hand,'Èesky=èervna'+CRLF)
    FWRITE(hand,'Slovenèina=júna'+CRLF)
	FWRITE(hand,'Bulgarian=þíè'+CRLF)
    FWRITE(hand,'Português=Junho'+CRLF)
    FWRITE(hand,'Latina=Iunius'+CRLF)
    FWRITE(hand,'Esperanto=Junio'+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cJuly]'+CRLF)
    FWRITE(hand,'xTrans=cJuly'+CRLF)
    FWRITE(hand,'English=July'+CRLF)
    FWRITE(hand,'Polski=lipca'+CRLF)
    FWRITE(hand,'Russian=èþëÿ'+CRLF)
    FWRITE(hand,'Italiano=Luglio'+CRLF)
    FWRITE(hand,'Deutsch=Juli'+CRLF)
    FWRITE(hand,'Espanol=Julio'+CRLF)
    FWRITE(hand,'Èesky=èervence'+CRLF)
    FWRITE(hand,'Slovenèina=júla'+CRLF)
	FWRITE(hand,'Bulgarian=þëè'+CRLF)
    FWRITE(hand,'Português=Julho'+CRLF)
    FWRITE(hand,'Latina=Iulius'+CRLF)
    FWRITE(hand,'Esperanto=Julio'+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cAugust]'+CRLF)
    FWRITE(hand,'xTrans=cAugust'+CRLF)
    FWRITE(hand,'English=August'+CRLF)
    FWRITE(hand,'Polski=sierpnia'+CRLF)
    FWRITE(hand,'Russian=àâãóñòà'+CRLF)
    FWRITE(hand,'Italiano=Agosto'+CRLF)
    FWRITE(hand,'Deutsch=August'+CRLF)
    FWRITE(hand,'Espanol=Agosto'+CRLF)
    FWRITE(hand,'Èesky=srpna'+CRLF)
    FWRITE(hand,'Slovenèina=augusta'+CRLF)
	FWRITE(hand,'Bulgarian=àâãóñò'+CRLF)
    FWRITE(hand,'Português=Agosto'+CRLF)
    FWRITE(hand,'Latina=Augustus'+CRLF)
    FWRITE(hand,'Esperanto=Augusto'+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cSeptember]'+CRLF)
    FWRITE(hand,'xTrans=cSeptember'+CRLF)
    FWRITE(hand,'English=September'+CRLF)
    FWRITE(hand,'Polski=wrzeœnia'+CRLF)
    FWRITE(hand,'Russian=ñåíòÿáðÿ'+CRLF)
    FWRITE(hand,'Italiano=Settembre'+CRLF)
    FWRITE(hand,'Deutsch=September'+CRLF)
    FWRITE(hand,'Espanol=Septiembre'+CRLF)
    FWRITE(hand,'Èesky=záøi'+CRLF)
    FWRITE(hand,'Slovenèina=septembra'+CRLF)
	FWRITE(hand,'Bulgarian=ñåïòåìâðè'+CRLF)
    FWRITE(hand,'Português=Setembro'+CRLF)
    FWRITE(hand,'Latina=September'+CRLF)
    FWRITE(hand,'Esperanto=Septembro'+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cOctober]'+CRLF)
    FWRITE(hand,'xTrans=cOctober'+CRLF)
    FWRITE(hand,'English=October'+CRLF)
    FWRITE(hand,'Polski=paŸdziernika'+CRLF)
    FWRITE(hand,'Russian=îêòÿáðÿ'+CRLF)
    FWRITE(hand,'Italiano=Ottobre'+CRLF)
    FWRITE(hand,'Deutsch=Oktober'+CRLF)
    FWRITE(hand,'Espanol=Octubre'+CRLF)
    FWRITE(hand,'Èesky=øíjna'+CRLF)
    FWRITE(hand,'Slovenèina=októbra'+CRLF)
	FWRITE(hand,'Bulgarian=îêòîìâðè'+CRLF)
    FWRITE(hand,'Português=Outubro'+CRLF)
    FWRITE(hand,'Latina=Oktobro'+CRLF)
    FWRITE(hand,'Esperanto=October'+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cNovember]'+CRLF)
    FWRITE(hand,'xTrans=cNovember'+CRLF)
    FWRITE(hand,'English=November'+CRLF)
    FWRITE(hand,'Polski=listopada'+CRLF)
    FWRITE(hand,'Russian=íîÿáðÿ'+CRLF)
    FWRITE(hand,'Italiano=Novembre'+CRLF)
    FWRITE(hand,'Deutsch=November'+CRLF)
    FWRITE(hand,'Espanol=Noviembre'+CRLF)
    FWRITE(hand,'Èesky=listopada'+CRLF)
    FWRITE(hand,'Slovenèina=novembra'+CRLF)
	FWRITE(hand,'Bulgarian=íîåìâðè'+CRLF)
    FWRITE(hand,'Português=Novembro'+CRLF)
    FWRITE(hand,'Latina=November'+CRLF)
    FWRITE(hand,'Esperanto=Novembro'+CRLF)
    FWRITE(hand,''+CRLF)
    FWRITE(hand,'[cDecember]'+CRLF)
    FWRITE(hand,'xTrans=cDecember'+CRLF)
    FWRITE(hand,'English=December'+CRLF)
    FWRITE(hand,'Polski=grudnia'+CRLF)
    FWRITE(hand,'Russian=äåêàáðÿ'+CRLF)
    FWRITE(hand,'Italiano=Dicembre'+CRLF)
    FWRITE(hand,'Deutsch=Dezember'+CRLF)
    FWRITE(hand,'Espanol=Diciembre'+CRLF)
    FWRITE(hand,'Èesky=prosince'+CRLF)
    FWRITE(hand,'Slovenèina=decembra'+CRLF)
	FWRITE(hand,'Bulgarian=äåêåìâðè'+CRLF)
    FWRITE(hand,'Português=Dezembro'+CRLF)
    FWRITE(hand,'Latina=December'+CRLF)
    FWRITE(hand,'Esperanto=Decembro'+CRLF)

    FCLOSE(hand)
  ENDIF
RETURN
